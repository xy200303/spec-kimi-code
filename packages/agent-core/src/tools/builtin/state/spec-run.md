Read the immutable snapshot of the approved spec coding run. Use this before or during implementation to keep work aligned with the approved goal, constraints, acceptance criteria, selected development strategy, quality gate, planned tasks, and verification approach.

This tool accepts no parameters and has no side effects. It is available only after `ExitPlanMode` has captured an approved spec run. If the plan is still being drafted or was not approved, revise the project-local specification and design documents instead.
